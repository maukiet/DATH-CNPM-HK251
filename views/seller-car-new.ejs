<%- include('partials/header') %>

<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
          <h4 class="mb-0">Đăng tin bán xe mới</h4>
        </div>
        <div class="card-body">
          <form action="/seller/cars/new"
                method="POST"
                enctype="multipart/form-data">
            <div class="row">
              <div class="col-md-6 mb-3">
                <label for="brand" class="form-label">Hãng xe *</label>
                <input type="text" class="form-control" id="brand" name="brand" required>
              </div>
              <div class="col-md-6 mb-3">
                <label for="model" class="form-label">Model *</label>
                <input type="text" class="form-control" id="model" name="model" required>
              </div>
            </div>

            <div class="row">
              <div class="col-md-4 mb-3">
                <label for="year" class="form-label">Năm sản xuất *</label>
                <input type="number" class="form-control" id="year" name="year" required>
              </div>
              <div class="col-md-4 mb-3">
                <label for="price" class="form-label">Giá bán (VNĐ) *</label>
                <input type="number" class="form-control" id="price" name="price" required>
              </div>
              <div class="col-md-4 mb-3">
                <label for="mileage" class="form-label">Số km đã đi (ODO)</label>
                <input type="number" class="form-control" id="mileage" name="mileage">
              </div>
            </div>

            <div class="mb-3">
              <label for="description" class="form-label">Mô tả chi tiết</label>
              <textarea class="form-control" id="description" name="description" rows="4"></textarea>
            </div>

            <hr class="my-3">

            <div class="mb-3">
              <label class="form-label d-block">Hình ảnh xe</label>

              <!-- Upload ảnh từ máy -->
              <div class="mb-2">
                <label for="image_file" class="form-label small">Upload ảnh từ máy</label>
                <input
                  type="file"
                  class="form-control"
                  id="image_file"
                  name="image_file"
                  accept="image/*"
                >
              </div>

              <div class="text-center my-2">
                <span class="small text-muted">Hoặc</span>
              </div>

              <!-- Hoặc dùng URL ảnh -->
              <div class="mb-2">
                <label for="image_url" class="form-label small">Dùng URL ảnh (tùy chọn)</label>
                <input
                  type="url"
                  class="form-control"
                  id="image_url"
                  name="image_url"
                  placeholder="https://link-den-anh-cua-ban.jpg"
                >
              </div>

              <div class="form-text">
                Ưu tiên ảnh upload từ máy. Nếu không upload, hệ thống sẽ dùng URL bạn nhập.
                Nếu cả hai đều trống, sẽ dùng ảnh mặc định.
              </div>
            </div>

            <div class="d-flex justify-content-between mt-4">
              <a href="/seller/cars" class="btn btn-outline-secondary">
                Quay lại danh sách xe
              </a>
              <button type="submit" class="btn btn-primary">
                Đăng tin
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
